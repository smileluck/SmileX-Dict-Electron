# 环境变量配置

在生产环境中，请创建 `.env` 文件并设置以下环境变量：

```bash
SECRET_KEY=your-very-secure-random-secret-key-here
```

生成安全的SECRET_KEY方法：

```python
import secrets
print(secrets.token_urlsafe(32))
```

## 开发环境警告

当前代码使用硬编码的SECRET_KEY仅用于开发，生产环境必须更改！

## 安全建议

1. 使用强随机SECRET_KEY
2. 设置合理的密码复杂度要求
3. 考虑添加登录失败次数限制
4. 添加验证码功能（可选）
5. 启用HTTPS
