# Plan: Clean Up Unused Code and Files

## Summary
Remove dead code, stale files, broken configurations, and build artifacts from the project.

---

## Phase 1: Delete Dead Code Files (Tracked in Git)

These files are committed to git but are completely unused:

| File | Reason |
|------|--------|
| `server/models.py` | Superseded by `server/app/models/` package |
| `server/auth.py` | Superseded by `server/app/dependencies.py` + `app/services/auth_service.py` |
| `server/db.py` | Superseded by `server/app/database.py` |
| `server/spider.py` | Duplicate of `server/app/services/lookup_service.py` |
| `src/features/session/sessionSlice.ts` | Registered in Redux store but none of its actions/selectors are used anywhere |
| `temp/models.py` | Legacy Peewee ORM + MySQL code from an old tech stack |

**Actions:**
1. Delete each file
2. Remove `sessionSlice` import and reducer from `src/store/index.ts`
3. Delete the `temp/` directory

## Phase 2: Remove Stale Documentation & Temp Files (Tracked in Git)

| File | Reason |
|------|--------|
| `plans/` (6 files) | Completed planning documents, no longer needed |
| `PASSWORD_REQUIREMENTS.md` | Feature spec for already-implemented feature |
| `PASSWORD_VALIDATION.md` | Feature spec for already-implemented feature |
| `server/scripts/start.sh` | Contains malformed Dockerfile content, not a valid shell script |

**Actions:**
1. Delete each file/directory

## Phase 3: Fix Corrupted Docker Configuration (Tracked in Git)

All four Docker files are broken and would fail immediately:

| File | Issue |
|------|--------|
| `Dockerfile` | References non-existent build stage, has `chmod +xx`, malformed JSON |
| `nginx.conf` | Contains Dockerfile content (`FROM python:3.13-slim`), not nginx config |
| `docker-compose.yml` | Port mismatch, copy-paste errors in volume mounts |
| `.dockerignore` | References non-existent `/static/` directory |

**Decision:** Delete all Docker files (user confirmed).

## Phase 4: Remove Build Artifacts & DB Files from Git Tracking

These files are tracked despite matching `.gitignore` rules (added before the rules existed):

| File | Reason |
|------|--------|
| `server/smilex.db` | Old stale database (61KB), app now uses `server/data/smilex.db` |
| `server/data/smilex.db` | Active database (5.5MB), should not be in git |
| `server/data/smilex.db-shm` | SQLite WAL artifact |
| `server/data/smilex.db-wal` | SQLite WAL artifact |
| `tsconfig.tsbuildinfo` | TypeScript incremental build cache |

**Actions:**
1. `git rm --cached` each file to stop tracking
2. Add `tsconfig.tsbuildinfo` and `.ruff_cache/` to `.gitignore`
3. The `.gitignore` already covers `*.db`, `*.db-wal`, `*.db-shm` so no changes needed there

## Phase 5: Delete Local-Only Artifacts (Untracked)

These exist on disk but are not tracked by git:

| Path | Reason |
|------|--------|
| `D:projectSmileXSmileX-Dictserverlogs/` | Empty directory with mangled path name |
| `server/D:projectSmileXSmileX-Dictservertest_output.txt` | Empty file with mangled path name |
| `srctypes/` | Empty directory, likely a typo for `src/types/` |
| `server/__pycache__/` | Bytecode cache for dead legacy files |
| `.ruff_cache/` | Ruff linter cache |
| `src/assets/` | Empty directory |

**Actions:**
1. Delete each file/directory

## Phase 6: Clean Up Unused Imports & Dead Code in Active Files

| File | Issue |
|------|--------|
| `src/App.tsx:5` | Unused import `useAppDispatch` |
| `src/App.tsx:44` | Duplicate `useDispatch()` call (`dictDispatch` is redundant with `dispatch` on line 39) |
| `src/routes/Dicts.tsx:3` | Unused imports: `addCustom`, `updateDict`, `removeDict` |
| `src/routes/Panel.tsx:75,95` | Duplicate `t('panel.daysOfWeek', ...)` calls (`dayNames` and `dayLabels`) |
| `src/features/dicts/dictsSlice.ts` | Unused export `addServerDict` action |

**Actions:**
1. Remove unused imports from each file
2. Remove redundant `dictDispatch` variable in App.tsx
3. Consolidate duplicate i18n call in Panel.tsx
4. Remove `addServerDict` action from dictsSlice.ts

## Phase 7: Update .gitignore

Add missing entries:
```
tsconfig.tsbuildinfo
.ruff_cache/
```

---

## Summary of Changes

| Category | Files Affected |
|----------|---------------|
| Dead code files deleted | 6 files |
| Stale docs deleted | 9 files (plans/ + 2 .md + start.sh) |
| Corrupted Docker files | 4 files (fix or delete - user choice) |
| Untracked from git | 5 files (db + tsbuildinfo) |
| Local artifacts deleted | 6 items |
| Active files cleaned | 4 files (unused imports, dead exports) |
| Config updated | 1 file (.gitignore) |
